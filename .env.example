# BBS API接続設定
BBS_API_BASE_URL=http://localhost:8787
BBS_API_BASE_PATH=/api/v1

# BBS側でENABLE_TURNSTILE=trueの場合はtrueに設定する
# trueにするとログイン画面にTurnstileセッションID入力欄が表示される
ENABLE_TURNSTILE=false

# アプリケーション設定
# セッション署名用シークレットキー (本番環境では必ず変更すること)
SECRET_KEY=changeme-in-production
LOG_LEVEL=INFO
